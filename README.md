# 종목 탐색기 (점수제 스크리닝 + 사후 성과 검증)

매일 아침 장 시작 전(07:45 KST), **코스피/코스닥 시가총액 상위 50%**를 대상으로
아래 8개 조건에 점수를 매기고, 합산 점수로 종목을 등급화합니다.
그렇게 뽑힌 픽들이 **1주일/1개월 후 실제로 어떻게 됐는지 자동으로 추적**합니다.

| 조건 | 배점 |
|---|---|
| MA5 > MA20 > MA60 (정배열) | +2 |
| MA20 상승 (오늘 20일선 > 어제 20일선) | +1 |
| MA60 상승 (오늘 60일선 > 어제 60일선) | +1 |
| RSI 35~50 구간 | +1 |
| RSI 상승 전환 (어제 30 이하 → 오늘 30 상향 돌파) | +2 |
| 이격도 저점 → 상승 전환 | +2 |
| RVOL(상대거래량) ≥ 2 | +2 |
| 거래대금 증가 (당일 > 20일 평균) | +1 |

**합계 12점 만점.** 8점 이상 **관심종목**, 10점 이상 **매수 후보**, 12점(전 조건 충족) **강한 매수 후보**로 분류합니다.

> ⚠️ 원래 조건표에는 VWAP·5분봉 고점 돌파·당일 상승 등 4개 조건이 더 있었지만,
> 이 4개는 **장이 열려야만 계산 가능한 장중 실시간 지표**라 장 시작 전(07:45) 실행 구조에서는
> 값 자체가 존재하지 않습니다. 그래서 이번 구현에서는 제외했습니다. 나중에 장중 실시간
> 모니터링을 별도로 만들면(예: `live-prices.yml`을 확장) 그때 추가로 합산할 수 있습니다.

> ⚠️ 이 도구는 조건에 맞는 종목을 기계적으로 찾아주는 스크리닝 도구이며,
> 매수/매도를 추천하는 것이 아닙니다. 투자 판단과 책임은 본인에게 있습니다.

---

## 1. 폴더 구조

```
stock-screener/
├── main.py                 # 메인 스크리닝 스크립트 (8개 조건 점수제 + 등급 + 히스토리 기록)
├── backtest.py              # 과거 데이터로 점수제 조합의 실제 예측력을 검증
├── evaluate_picks.py        # 누적된 픽을 1주일/1개월 후 실제 수익률로 검증
├── indicators.py            # RSI, 이격도, 이동평균 계산
├── naver_price.py           # 네이버 증권 일봉 시세 크롤링
├── naver_universe.py        # 네이버 증권 시가총액 순위 크롤링 (시장별 종목 목록)
├── naver_investor.py        # (현재 미사용) 외국인/기관 순매매 크롤링 — 나중에 재도입할 경우를 위해 보관
├── naver_shortsell.py       # (현재 미사용) 공매도 거래량 크롤링 — 나중에 재도입할 경우를 위해 보관
├── hankyung_flow.py          # 한국경제 페이지에서 코스피/코스닥 시장 전체 수급(실험적)
├── update_prices.py         # 장중 5분마다 현재가/지수만 빠르게 갱신
├── retry_util.py            # 요청 실패 시 재시도 헬퍼
├── notify_telegram.py       # (선택) 텔레그램 알림
├── requirements.txt
├── docs/
│   ├── index.html                # 아이폰에서 볼 모바일 웹페이지
│   ├── results.json              # 매일 자동 갱신되는 오늘의 스크리닝 결과
│   ├── live.json                 # 장중 5분마다 갱신되는 현재가/지수
│   ├── history.json              # 매일 누적되는 관심종목(8점) 이상 픽 기록
│   ├── performance_summary.json  # history.json을 집계한 성과 통계
│   └── backtest_result.json      # 백테스트 실행 결과
└── .github/workflows/
    ├── daily-screen.yml   # 매일 자동 실행 (스크리닝 + 사후검증 + 커밋을 한 번에 처리)
    ├── live-prices.yml    # 장중 5분마다 자동 실행 (현재가/지수)
    └── backtest.yml       # 수동 실행 전용 (과거 검증)
```

## 2. 셋업

1. GitHub에 새/기존 저장소 → 이 폴더 파일 전체 업로드
2. `.github/workflows/*.yml`이 안 올라갔다면, **Add file → Create new file**로
   경로를 직접 입력해서 만들기 (드래그 업로드 시 숨김 폴더가 자주 빠집니다)
3. **Settings → Pages** → Branch `main`, 폴더 `/docs` 선택 → Save
4. **Actions → Daily Stock Screening → Run workflow**
5. 완료되면 아이폰 사파리로 `https://<내계정>.github.io/<저장소이름>/` 접속 →
   공유 버튼(⬆️) → 홈 화면에 추가

## 3. 사후 성과 검증은 어떻게 동작하나요

- `main.py`가 매일 관심종목(8점) 이상 픽들을 `docs/history.json`에 날짜별로 누적 기록합니다.
- 같은 워크플로우 안에서 바로 이어서 `evaluate_picks.py`가 실행되어,
  **기록된 지 7일(1주) 또는 30일(1개월)이 지난 픽**들의 현재가를 조회해
  수익률을 계산하고 `docs/history.json`에 채워 넣습니다.
- 그 결과를 집계해 `docs/performance_summary.json`에 저장하고,
  화면 맨 위 "지난 픽 사후 성과" 카드에 평균 수익률/승률로 표시합니다.
- "1주/1개월"은 실제 거래일이 아니라 **달력 기준 날짜**로 계산합니다 (휴장일 캘린더 없이도
  안정적으로 동작하게 하기 위함).
- 데이터가 쌓이기까지 **최소 1주일 이상** 걸립니다 (그 전까지는 카드에 "데이터 쌓는 중"이라고 표시됩니다).

## 4. 스캔 범위 및 실행 시간

- **코스피/코스닥 시가총액 상위 50%**를 스캔합니다. 전체 상장 종목 목록을 먼저 가져온 뒤
  (`TOP_N_PER_MARKET=3000`으로 사실상 전체 목록 확보), 시가총액 내림차순으로 정렬된 그 목록에서
  앞쪽 절반만 잘라서 스캔합니다 (`SCAN_PERCENTAGE = 0.5`).
- 60일 이동평균 계산을 위해 종목당 시세를 넉넉히(70일치) 가져옵니다.
- 종목이 많은 만큼, 시세 조회는 **동시에 여러 종목씩 병렬로**(`SCAN_WORKERS`, 기본 8개) 처리합니다.
  (공매도 조회 단계가 없어져서, 이전 6개 지표 버전보다 오히려 더 단순하고 빨라졌습니다.)
- **실행 시간은 아직 실측 전이라 정확히 알 수 없습니다.** 상위 150개씩(총 300개)만 스캔했을 때는
  20~30분 정도였습니다. 시가총액 상위 50%(코스피+코스닥 합쳐 1000개 이상 추정)로 늘리면
  병렬 처리를 감안해도 그보다는 오래 걸릴 가능성이 높습니다. `.github/workflows/daily-screen.yml`의
  cron은 **한국시간 07:45**에 시작하도록 되어 있는데, 실행 시간이 너무 길어서 장 시작(09:00) 전에
  안 끝나면:
  - `SCAN_WORKERS` 값을 늘려서 병렬도를 높이거나
  - cron 시작 시각을 더 앞당기거나 (예: 전일 장마감 직후로)
  - `SCAN_PERCENTAGE` 값을 더 줄여서 스캔 범위를 좁히는
  조정이 필요할 수 있습니다. 처음 몇 번은 꼭 실제 소요 시간을 확인해보세요.
- **매매 타이밍 전략**: 전일 종가 기준으로 스크리닝하고, **장 시작(09:00) 동시호가/시가에 매수**한
  뒤 목표 수익률에 맞춰 매도 주문을 걸어두는 방식을 전제로 설계했습니다.

## 5. 장중 가격 갱신

전체 스크리닝과는 별도로, 이미 뽑힌 종목들의 **현재가/등락률**만 장중(09:00~15:45) 5분마다
`update_prices.py`가 갱신해 `docs/live.json`에 저장합니다. `docs/index.html`이 이 파일을
읽어서 화면에 표시합니다 (같은 사이트 내 파일이라 브라우저 CORS 제한이 없음).

## 6. 조건 값 조정

`main.py` 상단의 설정값으로 조정할 수 있습니다:

```python
DISPARITY_MA_PERIOD = 20
VOLUME_SURGE_MA_PERIOD = 20      # RVOL, 거래대금 평균 계산에 공용으로 사용
MA_SHORT, MA_MID, MA_LONG = 5, 20, 60
RSI_UPCROSS_LEVEL = 30           # RSI가 이 값을 상향 돌파하는 순간을 "상승 전환"으로 판단
RSI_ZONE_LOW = 35                # RSI 35~50 구간 하한
RSI_ZONE_HIGH = 50               # RSI 35~50 구간 상한
RVOL_THRESHOLD = 2.0             # RVOL(상대거래량) 기준치
TOP_N_PER_MARKET = 3000          # 시장 전체 목록을 가져오기 위한 값
SCAN_PERCENTAGE = 0.5            # 그중 시가총액 상위 몇 %만 실제로 스캔할지
HALT_CHECK_DAYS = 3              # 최근 이 기간 중 거래량 0인 날이 있으면 거래정지로 보고 제외
SCAN_WORKERS = 8                 # 시세 조회 동시 요청 수 (너무 높이면 차단 위험)
HISTORY_MAX_ENTRIES = 200        # history.json에 보관할 최대 일수

TIER_STRONG_BUY = 12   # 강한 매수 후보
TIER_BUY_CANDIDATE = 10  # 매수 후보
TIER_WATCH = 8          # 관심종목
```

각 조건의 배점은 `main.py`의 `SCORE_RULES` 리스트에서 조정할 수 있습니다
(조건 키, 표시 이름, 배점 순서로 되어 있습니다). 배점을 바꾸면 `backtest.py`도
같은 `SCORE_RULES`를 그대로 가져다 쓰므로 자동으로 함께 반영됩니다.

**거래정지 종목 필터링**: 최근 `HALT_CHECK_DAYS`일 중 단 하루라도 거래량이 0이면 그 종목은
스캔에서 완전히 제외됩니다. 거래정지 중엔 가격이 안 움직여 지표가 왜곡되고,
거래 재개 시 액면분할·감자 등으로 가격이 급변해 "폭락"처럼 잘못 잡히는 문제가 있었기 때문입니다
(실제 사례: 거래정지 상태였던 종목이 재개되며 액면분할로 종가가 반토막 나 -59% 폭락처럼 표시된 적 있음).

`evaluate_picks.py` 상단의 `HORIZONS = {"1w": 7, "1m": 30}` 값을 바꾸면
평가 시점(며칠 후 수익률을 볼지)을 조정할 수 있습니다.

## 7. 화면 표시 관련 참고

- 8개 조건의 합산 점수에 따라 **관심종목(8점+) / 매수 후보(10점+) / 강한 매수 후보(12점)**
  세 등급으로 나눠서 표시합니다. 점수가 높을수록 위쪽(더 눈에 띄는 스타일)에 표시됩니다.
- 각 종목 카드에는 **몇 점을 받았는지**와 **어떤 조건들 때문에 그 점수를 받았는지**(예: "정배열 +2",
  "RVOL≥2 +2")가 태그로 함께 표시됩니다.
- **화면 최하단에는 "8개 조건 중 2개 이상 충족"** 리스트가 따로 있습니다. 위 3개 등급은 점수(배점 합)
  기준이라, 배점이 작은 조건 여러 개가 겹쳐도 등급 문턱을 못 넘을 수 있습니다. 이 리스트는 점수와
  무관하게 **조건을 몇 개나 만족했는지**만 보고, 2개 이상이면 전부 보여줍니다 (등급 리스트와 겹칠 수 있음).
- **"📅 지난 날짜 기록 보기"**를 누르면 날짜를 선택할 수 있고, 그 날짜에 관심종목(8점) 이상으로
  뽑혔던 종목들과 **그날의 등락률, 점수, 조건 태그**를 바로 볼 수 있습니다. 매일 계속 종목을
  지켜보지 않아도 "이 조건이 그동안 얼마나 맞았는지"를 나중에 훑어보기 편하게 만든 기능입니다.
  `docs/history.json`을 그대로 읽어서 보여주며, 데이터는 `main.py`가 매일 자동으로 쌓습니다.
- 각 등급 리스트는 우측에 전일 대비 등락률을 크게 표시하며,
  장중에는 5분마다 실시간 등락률로 자동 갱신됩니다.
- 상단에 코스피/코스닥 지수 현재가 + 최근 추이 그래프가 표시됩니다 (장중 5분 간격 데이터 기준).
- 지수 카드에 **외인/기관/개인 시장 전체 순매매(억원)**도 함께 표시됩니다. 이 데이터는 네이버가 아닌
  한국경제(markets.hankyung.com) 페이지에서 가져오는 **실험적 기능**이라, 페이지 구조가 바뀌면
  조용히 실패하고 그 부분만 안 보일 수 있습니다 (다른 기능에는 영향 없음).

## 8. 백테스트 (과거 검증)

점수제 조건 조합이 실제로 상승 신호였는지, 과거 데이터로 검증할 수 있습니다.
`main.py`와 완전히 동일한 `SCORE_RULES`/등급 기준을 재사용하므로, 배점을 바꾸면
백테스트에도 자동으로 반영됩니다 (교차 검증 완료: 여러 시드의 합성 데이터로
`main.py`와 `backtest.py`의 점수가 항상 정확히 일치하는 것을 확인함).

- **Actions → Backtest (과거 검증) → Run workflow**로 수동 실행 (자동 스케줄 없음)
- `top_n`(시장당 평가 종목 수), `eval_days`(평가할 과거 거래일 수)를 입력받아 실행
- 결과는 `docs/backtest_result.json`에 저장되고, 실행 로그에도 요약이 출력됩니다:
  **관심종목(8점+) / 매수후보(10점+) / 강한매수후보(12점)** 세 등급별로,
  그 등급에 든 종목들의 1주/1개월 후 평균 수익률 vs 그날 스캔된 전체 종목 평균 수익률(베이스라인) 비교
- **한계**: 종목 유니버스는 "오늘 기준" 시가총액 상위 N이라 과거 시점 실제 상위 N과 다를 수 있음
  (생존편향 가능)
- 종목 수 x 평가 기간이 클수록 네이버 요청이 많아져 오래 걸리므로, 처음엔 기본값(40종목/40거래일)으로
  먼저 실행해보고 필요시 늘릴 것
